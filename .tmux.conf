# remap prefix
set -g prefix C-a
bind C-a send-prefix
unbind C-b

# quality of life
set -g history-limit 10000
set -g allow-rename off

# Search mode VI
set-window-option -g mode-keys vi

# Uncomment line below if logging of console output is required
#run-shell /opt/tmux-logging/logging.tmux
